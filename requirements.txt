# ── Core (required) ───────────────────────────────────────────────
# SQLCipher Python wrapper — pre-built wheel for Windows
# Replaces the sqlcipher binary used in the Mac version
sqlcipher3-binary

# Message content decompression (some auxiliary data uses zstd)
zstandard

# Process enumeration for key extraction
psutil

# ── Step 3: Voice transcription (optional) ───────────────────────
openai-whisper

# ── Step 4: AI bot (optional) ────────────────────────────────────
anthropic
watchdog
sentence-transformers
