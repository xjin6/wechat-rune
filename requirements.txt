# ── Core (required) ───────────────────────────────────────────────
# SQLCipher Python wrapper — pre-built wheel for Windows
sqlcipher3-binary

# Message content decompression
zstandard

# Process enumeration for key extraction
psutil

# ── Step 3: Voice transcription (optional) ───────────────────────
openai-whisper

# Windows SILK audio decoder (replaces the compiled silk-v3-decoder binary on Mac)
pilk

# ── Step 4: AI bot + voice correction (optional) ─────────────────
anthropic
watchdog
sentence-transformers
